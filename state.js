const cart = {
  items: [],
};

export const addToCart = (item) => {
  console.log(item);
};

export const removeFromCart = () => {};

export const getCartItems = () => {
  return cart.items;
};
